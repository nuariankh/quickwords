import { DefaultTheme } from "react-native-paper";

const appTheme = {
    ...DefaultTheme,
    colors: {
        primary: '#FFCC00',
        secondary: '#6C757D',
        tertiary: '#800020'
    }
}

export default appTheme;